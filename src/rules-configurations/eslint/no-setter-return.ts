import Configuration from './no-setter-return.d'

const configuration:Configuration = {
	ruleId: `no-setter-return`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
