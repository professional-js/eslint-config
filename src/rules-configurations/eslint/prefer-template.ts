import Configuration from './prefer-template.d'

const configuration:Configuration = {
	ruleId: `prefer-template`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [],
}

export default configuration
