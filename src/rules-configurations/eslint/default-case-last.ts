import Configuration from './default-case-last.d'

const configuration:Configuration = {
	ruleId: `default-case-last`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
