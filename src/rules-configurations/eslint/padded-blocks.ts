import Configuration from './padded-blocks.d'

const configuration:Configuration = {
	ruleId: `padded-blocks`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [ `never` ],
}

export default configuration
