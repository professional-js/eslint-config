import Configuration from './no-undefined.d'

const configuration:Configuration = {
	ruleId: `no-undefined`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: false,
}

export default configuration
