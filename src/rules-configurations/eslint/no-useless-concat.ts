import Configuration from './no-useless-concat.d'

const configuration:Configuration = {
	ruleId: `no-useless-concat`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
