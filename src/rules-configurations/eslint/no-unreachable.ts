import Configuration from './no-unreachable.d'

const configuration:Configuration = {
	ruleId: `no-unreachable`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
