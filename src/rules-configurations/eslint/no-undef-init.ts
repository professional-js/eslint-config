import Configuration from './no-undef-init.d'

const configuration:Configuration = {
	ruleId: `no-undef-init`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: false,
}

export default configuration
