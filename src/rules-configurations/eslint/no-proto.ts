import Configuration from './no-proto.d'

const configuration:Configuration = {
	ruleId: `no-proto`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
