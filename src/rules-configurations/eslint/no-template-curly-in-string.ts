import Configuration from './no-template-curly-in-string.d'

const configuration:Configuration = {
	ruleId: `no-template-curly-in-string`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
