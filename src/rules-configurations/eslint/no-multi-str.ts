import Configuration from './no-multi-str.d'

const configuration:Configuration = {
	ruleId: `no-multi-str`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
