import Configuration from './no-unsafe-finally.d'

const configuration:Configuration = {
	ruleId: `no-unsafe-finally`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
