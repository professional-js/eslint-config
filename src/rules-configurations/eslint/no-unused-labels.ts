import Configuration from './no-unused-labels.d'

const configuration:Configuration = {
	ruleId: `no-unused-labels`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
