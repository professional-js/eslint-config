import Configuration from './no-misleading-character-class.d'

const configuration:Configuration = {
	ruleId: `no-misleading-character-class`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
