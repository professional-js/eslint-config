import Configuration from './no-dupe-else-if.d'

const configuration:Configuration = {
	ruleId: `no-dupe-else-if`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
