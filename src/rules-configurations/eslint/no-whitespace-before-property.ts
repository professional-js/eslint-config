import Configuration from './no-whitespace-before-property.d'

const configuration:Configuration = {
	ruleId: `no-whitespace-before-property`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [],
}

export default configuration
