import Configuration from './no-iterator.d'

const configuration:Configuration = {
	ruleId: `no-iterator`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
