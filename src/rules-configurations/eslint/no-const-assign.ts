import Configuration from './no-const-assign.d'

const configuration:Configuration = {
	ruleId: `no-const-assign`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
