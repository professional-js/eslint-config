import Configuration from './no-import-assign.d'

const configuration:Configuration = {
	ruleId: `no-import-assign`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
