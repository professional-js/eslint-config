import Configuration from './no-prototype-builtins.d'

const configuration:Configuration = {
	ruleId: `no-prototype-builtins`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
