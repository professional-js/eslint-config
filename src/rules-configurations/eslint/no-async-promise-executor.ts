import Configuration from './no-async-promise-executor.d'

const configuration:Configuration = {
	ruleId: `no-async-promise-executor`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
