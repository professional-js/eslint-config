import Configuration from './prefer-regex-literals.d'

const configuration:Configuration = {
	ruleId: `prefer-regex-literals`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
