import Configuration from './no-empty-pattern.d'

const configuration:Configuration = {
	ruleId: `no-empty-pattern`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
