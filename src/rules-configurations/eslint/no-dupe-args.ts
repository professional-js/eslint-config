import Configuration from './no-dupe-args.d'

const configuration:Configuration = {
	ruleId: `no-dupe-args`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
