import Configuration from './no-labels.d'

const configuration:Configuration = {
	ruleId: `no-labels`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [],
}

export default configuration
