import Configuration from './no-self-compare.d'

const configuration:Configuration = {
	ruleId: `no-self-compare`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
