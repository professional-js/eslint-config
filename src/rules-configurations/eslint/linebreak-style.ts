import Configuration from './linebreak-style.d'

const configuration:Configuration = {
	ruleId: `linebreak-style`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [ `unix` ],
}

export default configuration
