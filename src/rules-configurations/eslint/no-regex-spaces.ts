import Configuration from './no-regex-spaces.d'

const configuration:Configuration = {
	ruleId: `no-regex-spaces`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
