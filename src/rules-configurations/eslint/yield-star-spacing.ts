import Configuration from './yield-star-spacing.d'

const configuration:Configuration = {
	ruleId: `yield-star-spacing`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [ `both` ],
}

export default configuration
