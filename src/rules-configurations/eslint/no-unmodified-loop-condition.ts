import Configuration from './no-unmodified-loop-condition.d'

const configuration:Configuration = {
	ruleId: `no-unmodified-loop-condition`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
