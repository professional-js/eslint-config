import Configuration from './no-with.d'

const configuration:Configuration = {
	ruleId: `no-with`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
