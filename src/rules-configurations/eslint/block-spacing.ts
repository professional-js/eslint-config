import Configuration from './block-spacing.d'

const configuration:Configuration = {
	ruleId: `block-spacing`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [ `always` ],
}

export default configuration
