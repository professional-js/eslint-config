import Configuration from './require-yield.d'

const configuration:Configuration = {
	ruleId: `require-yield`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
