import Configuration from './no-alert.d'

const configuration:Configuration = {
	ruleId: `no-alert`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
