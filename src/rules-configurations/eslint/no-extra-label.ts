import Configuration from './no-extra-label.d'

const configuration:Configuration = {
	ruleId: `no-extra-label`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
