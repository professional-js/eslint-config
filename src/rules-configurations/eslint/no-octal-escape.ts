import Configuration from './no-octal-escape.d'

const configuration:Configuration = {
	ruleId: `no-octal-escape`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
