import Configuration from './require-atomic-updates.d'

const configuration:Configuration = {
	ruleId: `require-atomic-updates`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
