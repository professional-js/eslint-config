import Configuration from './constructor-super.d'

const configuration:Configuration = {
	ruleId: `constructor-super`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
