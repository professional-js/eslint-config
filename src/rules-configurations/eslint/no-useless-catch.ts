import Configuration from './no-useless-catch.d'

const configuration:Configuration = {
	ruleId: `no-useless-catch`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
