import Configuration from './no-floating-decimal.d'

const configuration:Configuration = {
	ruleId: `no-floating-decimal`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
