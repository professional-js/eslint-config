import Configuration from './no-new-symbol.d'

const configuration:Configuration = {
	ruleId: `no-new-symbol`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
