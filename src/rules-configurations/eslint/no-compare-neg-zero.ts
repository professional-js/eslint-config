import Configuration from './no-compare-neg-zero.d'

const configuration:Configuration = {
	ruleId: `no-compare-neg-zero`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
