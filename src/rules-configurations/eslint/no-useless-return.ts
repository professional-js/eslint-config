import Configuration from './no-useless-return.d'

const configuration:Configuration = {
	ruleId: `no-useless-return`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
