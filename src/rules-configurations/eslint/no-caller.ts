import Configuration from './no-caller.d'

const configuration:Configuration = {
	ruleId: `no-caller`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
