import Configuration from './no-shadow-restricted-names.d'

const configuration:Configuration = {
	ruleId: `no-shadow-restricted-names`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
