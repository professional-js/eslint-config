import Configuration from './no-ex-assign.d'

const configuration:Configuration = {
	ruleId: `no-ex-assign`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
