import Configuration from './no-empty-character-class.d'

const configuration:Configuration = {
	ruleId: `no-empty-character-class`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
