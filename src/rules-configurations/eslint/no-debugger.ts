import Configuration from './no-debugger.d'

const configuration:Configuration = {
	ruleId: `no-debugger`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
