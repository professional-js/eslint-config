import Configuration from './no-new-func.d'

const configuration:Configuration = {
	ruleId: `no-new-func`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
