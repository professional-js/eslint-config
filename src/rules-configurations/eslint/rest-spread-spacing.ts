import Configuration from './rest-spread-spacing.d'

const configuration:Configuration = {
	ruleId: `rest-spread-spacing`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [ `never` ],
}

export default configuration
