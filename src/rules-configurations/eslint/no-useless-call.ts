import Configuration from './no-useless-call.d'

const configuration:Configuration = {
	ruleId: `no-useless-call`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
