import Configuration from './no-sparse-arrays.d'

const configuration:Configuration = {
	ruleId: `no-sparse-arrays`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
