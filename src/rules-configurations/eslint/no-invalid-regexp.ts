import Configuration from './no-invalid-regexp.d'

const configuration:Configuration = {
	ruleId: `no-invalid-regexp`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
