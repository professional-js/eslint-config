import Configuration from './no-new-wrappers.d'

const configuration:Configuration = {
	ruleId: `no-new-wrappers`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
