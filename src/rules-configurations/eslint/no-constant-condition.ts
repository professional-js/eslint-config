import Configuration from './no-constant-condition.d'

const configuration:Configuration = {
	ruleId: `no-constant-condition`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
