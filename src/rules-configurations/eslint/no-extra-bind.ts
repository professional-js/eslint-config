import Configuration from './no-extra-bind.d'

const configuration:Configuration = {
	ruleId: `no-extra-bind`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
