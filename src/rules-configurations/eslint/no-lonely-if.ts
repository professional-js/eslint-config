import Configuration from './no-lonely-if.d'

const configuration:Configuration = {
	ruleId: `no-lonely-if`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
