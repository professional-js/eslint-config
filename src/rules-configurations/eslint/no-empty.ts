import Configuration from './no-empty.d'

const configuration:Configuration = {
	ruleId: `no-empty`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
