import Configuration from './no-obj-calls.d'

const configuration:Configuration = {
	ruleId: `no-obj-calls`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
