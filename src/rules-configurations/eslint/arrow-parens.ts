import Configuration from './arrow-parens.d'

const configuration:Configuration = {
	ruleId: `arrow-parens`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [ `always` ],
}

export default configuration
