import Configuration from './eol-last.d'

const configuration:Configuration = {
	ruleId: `eol-last`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [ `always` ],
}

export default configuration
