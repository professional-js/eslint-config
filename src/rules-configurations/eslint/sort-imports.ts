import Configuration from './sort-imports.d'

const configuration:Configuration = {
	ruleId: `sort-imports`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: false,
}

export default configuration
