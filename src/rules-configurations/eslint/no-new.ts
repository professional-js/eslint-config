import Configuration from './no-new.d'

const configuration:Configuration = {
	ruleId: `no-new`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
