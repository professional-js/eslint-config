import Configuration from './space-before-blocks.d'

const configuration:Configuration = {
	ruleId: `space-before-blocks`,
	providerId: `eslint`,
	priority: `TASTE`,
	activate: true,
	options: [ `always` ],
}

export default configuration
