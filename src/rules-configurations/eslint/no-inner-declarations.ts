import Configuration from './no-inner-declarations.d'

const configuration:Configuration = {
	ruleId: `no-inner-declarations`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: false,
}

export default configuration
