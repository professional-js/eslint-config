import Configuration from './no-dupe-keys.d'

const configuration:Configuration = {
	ruleId: `no-dupe-keys`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
