import Configuration from './eqeqeq.d'

const configuration:Configuration = {
	ruleId: `eqeqeq`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [ `always` ],
}

export default configuration
