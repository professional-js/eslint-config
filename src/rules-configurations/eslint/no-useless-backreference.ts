import Configuration from './no-useless-backreference.d'

const configuration:Configuration = {
	ruleId: `no-useless-backreference`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
