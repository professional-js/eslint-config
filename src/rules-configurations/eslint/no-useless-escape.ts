import Configuration from './no-useless-escape.d'

const configuration:Configuration = {
	ruleId: `no-useless-escape`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
