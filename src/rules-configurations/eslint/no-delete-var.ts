import Configuration from './no-delete-var.d'

const configuration:Configuration = {
	ruleId: `no-delete-var`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
