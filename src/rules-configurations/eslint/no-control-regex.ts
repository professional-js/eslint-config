import Configuration from './no-control-regex.d'

const configuration:Configuration = {
	ruleId: `no-control-regex`,
	providerId: `eslint`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
