import Configuration from './no-lone-blocks.d'

const configuration:Configuration = {
	ruleId: `no-lone-blocks`,
	providerId: `eslint`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
