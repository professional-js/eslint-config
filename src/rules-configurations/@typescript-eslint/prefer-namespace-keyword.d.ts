import { RuleConfiguration } from '../../../support/Rule.d'

export type Options = Array<never>

type Configuration = RuleConfiguration<'@typescript-eslint/prefer-namespace-keyword', '@typescript-eslint/eslint-plugin', Options>

export default Configuration
