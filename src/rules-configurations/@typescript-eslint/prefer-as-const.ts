import Configuration from './prefer-as-const.d'

const configuration:Configuration = {
	ruleId: `@typescript-eslint/prefer-as-const`,
	providerId: `@typescript-eslint/eslint-plugin`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
