import { RuleConfiguration } from '../../../support/Rule.d'

export type Options = Array<never>

type Configuration = RuleConfiguration<'@typescript-eslint/no-for-in-array', '@typescript-eslint/eslint-plugin', Options>

export default Configuration
