import Configuration from './exports.d'

const configuration:Configuration = {
	ruleId: `simple-import-sort/exports`,
	providerId: `eslint-plugin-simple-import-sort`,
	priority: `TASTE`,
	activate: true,
	options: [],
}

export default configuration
