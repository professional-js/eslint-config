import Configuration from './export.d'

const configuration:Configuration = {
	ruleId: `import/export`,
	providerId: `eslint-plugin-import`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
