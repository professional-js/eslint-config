import Configuration from './no-named-as-default.d'

const configuration:Configuration = {
	ruleId: `import/no-named-as-default`,
	providerId: `eslint-plugin-import`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
