import Configuration from './default.d'

const configuration:Configuration = {
	ruleId: `import/default`,
	providerId: `eslint-plugin-import`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
