import Configuration from './named.d'

const configuration:Configuration = {
	ruleId: `import/named`,
	providerId: `eslint-plugin-import`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
