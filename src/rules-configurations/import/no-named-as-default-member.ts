import Configuration from './no-named-as-default-member.d'

const configuration:Configuration = {
	ruleId: `import/no-named-as-default-member`,
	providerId: `eslint-plugin-import`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
