import Configuration from './compat.d'

const configuration:Configuration = {
	ruleId: `compat/compat`,
	providerId: `eslint-plugin-compat`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
