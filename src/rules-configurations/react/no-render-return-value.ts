import Configuration from './no-render-return-value.d'

const configuration:Configuration = {
	ruleId: `react/no-render-return-value`,
	providerId: `eslint-plugin-react`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
