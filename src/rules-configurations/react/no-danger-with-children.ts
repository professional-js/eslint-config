import Configuration from './no-danger-with-children.d'

const configuration:Configuration = {
	ruleId: `react/no-danger-with-children`,
	providerId: `eslint-plugin-react`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
