import Configuration from './no-unknown-property.d'

const configuration:Configuration = {
	ruleId: `react/no-unknown-property`,
	providerId: `eslint-plugin-react`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
