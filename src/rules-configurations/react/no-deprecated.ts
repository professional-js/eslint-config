import Configuration from './no-deprecated.d'

const configuration:Configuration = {
	ruleId: `react/no-deprecated`,
	providerId: `eslint-plugin-react`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
