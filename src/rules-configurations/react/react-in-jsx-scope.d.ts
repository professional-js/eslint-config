import { RuleConfiguration } from '../../../support/Rule.d'

export type Options = Array<never>

type Configuration = RuleConfiguration<'react/react-in-jsx-scope', 'eslint-plugin-react', Options>

export default Configuration
