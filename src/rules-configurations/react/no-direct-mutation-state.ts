import Configuration from './no-direct-mutation-state.d'

const configuration:Configuration = {
	ruleId: `react/no-direct-mutation-state`,
	providerId: `eslint-plugin-react`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
