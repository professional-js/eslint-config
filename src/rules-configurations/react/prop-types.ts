import Configuration from './prop-types.d'

const configuration:Configuration = {
	ruleId: `react/prop-types`,
	providerId: `eslint-plugin-react`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
