import Configuration from './display-name.d'

const configuration:Configuration = {
	ruleId: `react/display-name`,
	providerId: `eslint-plugin-react`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
