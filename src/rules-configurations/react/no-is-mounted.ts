import Configuration from './no-is-mounted.d'

const configuration:Configuration = {
	ruleId: `react/no-is-mounted`,
	providerId: `eslint-plugin-react`,
	priority: `HELPFUL`,
	activate: true,
	options: [],
}

export default configuration
