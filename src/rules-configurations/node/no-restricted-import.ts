import Configuration from './no-restricted-import.d'

const configuration:Configuration = {
	ruleId: `node/no-restricted-import`,
	providerId: `eslint-plugin-node`,
	ignore: true,
}

export default configuration
