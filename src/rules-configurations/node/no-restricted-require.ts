import Configuration from './no-restricted-require.d'

const configuration:Configuration = {
	ruleId: `node/no-restricted-require`,
	providerId: `eslint-plugin-node`,
	ignore: true,
}

export default configuration
