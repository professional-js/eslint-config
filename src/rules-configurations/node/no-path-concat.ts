import Configuration from './no-path-concat.d'

const configuration:Configuration = {
	ruleId: `node/no-path-concat`,
	providerId: `eslint-plugin-node`,
	priority: `IMPORTANT`,
	activate: true,
	options: [],
}

export default configuration
